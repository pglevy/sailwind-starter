# S2: App Spec

## Prompt

Build a Sailwind prototype based on the following app specification. Create all pages, data entities, and routes described in the spec.

Follow the project conventions in AGENTS.md — use `@pglevy/sailwind` components, put data in `src/db/`, register routes in `src/App.tsx`, and add links to `src/pages/home.tsx`.

## Attached

- `spec.md`
