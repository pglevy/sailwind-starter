# S8: SAIL Expression Only

## Prompt

Convert the following SAIL expression into a Sailwind prototype page. Map each SAIL component to its closest Sailwind equivalent.

Use the image assets provided in the `assets/` folder for any user images or document images referenced in the expression. These have been copied to `public/` and can be referenced as `<filename>`.

Build the page using `@pglevy/sailwind` components following the project conventions in AGENTS.md.

## Attached

- `source.sail`
